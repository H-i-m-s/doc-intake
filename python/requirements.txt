# doc-intake Python 依赖
# 安装: pip install -r requirements.txt

# ===== 核心依赖（必装，缺任何一个主要功能都跑不起来）=====

Pillow>=9.0.0           # 图片处理：长图分割、EMF→PNG 转换、图片读写
PyMuPDF>=1.23.0         # PDF 本地兜底提取 + 大 PDF 按页切分（import fitz）
requests>=2.28.0        # HTTP 请求：MinerU / PaddleOCR 云端 API、远程图片下载、HTML 抓取

# ===== 可选依赖（缺了对应功能会降级，不阻塞主流程）=====

# MinerU 云端 PDF 提取（不装则 PDF 降级链跳过 mineru 档）
mineru-open-sdk>=0.2.0

# HTML → Markdown 转换（不装则 HTML 解析返回错误提示）
html-to-markdown>=0.5.0

# MathType 公式提取（不装则 MathType 公式退化为图片，OMML 公式不受影响）
mtef-py>=1.0.0

# ===== Windows 专用（旧版 Office 格式需要）=====

# 旧版 .doc/.xls/.ppt 文件转 .docx/.xlsx/.pptx（通过 Office COM 自动化）
# 不装则旧版 Office 文件返回转换器不可用；现代 OOXML 文件不受影响
pywin32>=300; sys_platform == "win32"
