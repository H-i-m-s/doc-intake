from setuptools import setup, find_packages

setup(
    name="mtef-py",
    version="1.0.0",
    description="MathType MTEF format parser, converts to LaTeX",
    packages=find_packages(),
    python_requires=">=3.10",
)